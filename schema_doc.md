# Database Schema Documentation

> Auto-generated by GitHub Actions.  
> Source: Supabase `public` schema via `supabase db dump` (schema hash: `c1735f36`).

## Tables

- [`activities`](#activities)
- [`board_collaborators`](#board_collaborators)
- [`board_invitations`](#board_invitations)
- [`board_lists`](#board_lists)
- [`board_template_lists`](#board_template_lists)
- [`board_template_tasks`](#board_template_tasks)
- [`board_templates`](#board_templates)
- [`boards`](#boards)
- [`task_activities`](#task_activities)
- [`tasks`](#tasks)
- [`user_preferences`](#user_preferences)

---

## `activities`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `name` | `character varying` | ❌ | — | — |
| `data` | `"jsonb"` | ✅ | `'[]'::"jsonb"` | — |
| `media` | `"jsonb"` | ✅ | `'[]'::"jsonb"` | — |
| `user_id` | `"uuid"` | ✅ | — | FK → `auth.users(id)` ON DELETE CASCADE |
| `created_at` | `timestamp with time zone` | ❌ | `"now"()` | — |

### RLS Policies

- DELETE: Collaborators can delete shared board activities
- DELETE: Users can delete their own activities
- INSERT: Collaborators can insert shared board activities
- INSERT: Users can insert their own activities
- SELECT: Collaborators can view shared board activities
- SELECT: Users can view their own activities
- UPDATE: Collaborators can update shared board activities
- UPDATE: Users can update their own activities

---

## `board_collaborators`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `board_id` | `bigint` | ❌ | — | FK → `public.boards(id)` ON DELETE CASCADE |
| `user_id` | `"uuid"` | ❌ | — | FK → `auth.users(id)` ON DELETE CASCADE |
| `role` | `"public"."collaborator_role"` | ❌ | `'viewer'::"public"."collaborator_role"` | — |
| `invited_by` | `"uuid"` | ✅ | — | FK → `auth.users(id)` ON DELETE SET NULL |
| `status` | `"public"."invitation_status"` | ❌ | `'pending'::"public"."invitation_status"` | — |
| `created_at` | `timestamp with time zone` | ❌ | `"now"()` | — |
| `accepted_at` | `timestamp with time zone` | ✅ | — | — |

### RLS Policies

- DELETE: Board owners and collaborators can delete collaborators
- INSERT: Board owners can insert collaborators
- SELECT: Board owners can view collaborators
- UPDATE: Board owners can update collaborators
- UPDATE: Invitees can update their own collaboration status

---

## `board_invitations`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `board_id` | `bigint` | ❌ | — | FK → `public.boards(id)` ON DELETE CASCADE |
| `email` | `character varying(255)` | ❌ | — | — |
| `role` | `"public"."collaborator_role"` | ❌ | `'viewer'::"public"."collaborator_role"` | — |
| `invited_by` | `"uuid"` | ❌ | — | FK → `auth.users(id)` ON DELETE CASCADE |
| `token` | `"uuid"` | ❌ | `"gen_random_uuid"()` | — |
| `created_at` | `timestamp with time zone` | ❌ | `"now"()` | — |
| `expires_at` | `timestamp with time zone` | ❌ | `("now"() + '7 days'::interval)` | — |
| `status` | `"public"."invitation_status"` | ❌ | `'pending'::"public"."invitation_status"` | — |

### RLS Policies

- DELETE: Board owners can delete invitations
- INSERT: Board owners can insert invitations
- SELECT: Board owners can view invitations
- SELECT: Invitees can view their own invitations
- UPDATE: Board owners can update invitations

---

## `board_lists`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `board_id` | `bigint` | ❌ | — | FK → `public.boards(id)` ON DELETE CASCADE |
| `name` | `character varying` | ❌ | — | — |
| `position` | `smallint` | ❌ | `0` | — |
| `user_id` | `"uuid"` | ✅ | — | FK → `auth.users(id)` |
| `created_at` | `timestamp with time zone` | ❌ | `"now"()` | — |

### RLS Policies

- DELETE: Collaborators can delete shared board lists
- DELETE: Users can delete their own board lists
- INSERT: Collaborators can insert shared board lists
- INSERT: Users can insert their own board lists
- SELECT: Collaborators can view shared board lists
- SELECT: Users can view their own board lists
- UPDATE: Collaborators can update shared board lists
- UPDATE: Users can update their own board lists

---

## `board_template_lists`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `template_id` | `bigint` | ❌ | — | FK → `public.board_templates(id)` ON DELETE CASCADE |
| `name` | `character varying` | ❌ | — | — |
| `list_type` | `smallint` | ✅ | — | — |
| `position` | `smallint` | ❌ | `0` | — |

### RLS Policies

- SELECT: Read lists of visible templates

---

## `board_template_tasks`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `template_id` | `bigint` | ❌ | — | FK → `public.board_templates(id)` ON DELETE CASCADE |
| `template_list_id` | `bigint` | ❌ | — | FK → `public.board_template_lists(id)` ON DELETE CASCADE |
| `name` | `character varying` | ❌ | — | — |
| `description` | `"text"` | ✅ | — | — |
| `position` | `smallint` | ❌ | `0` | — |
| `activities` | `"jsonb"` | ❌ | `'[]'::"jsonb"` | — |

### RLS Policies

- SELECT: Read tasks of visible templates

---

## `board_templates`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `name` | `character varying` | ❌ | — | — |
| `description` | `"text"` | ✅ | — | — |
| `is_system` | `boolean` | ❌ | `false` | — |
| `user_id` | `"uuid"` | ✅ | — | FK → `auth.users(id)` ON DELETE CASCADE |
| `created_at` | `timestamp with time zone` | ❌ | `"now"()` | — |

### RLS Policies

- DELETE: Delete own templates
- INSERT: Insert own templates
- SELECT: Read system or own templates
- UPDATE: Update own templates

---

## `boards`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK |
| `name` | `character varying(255)` | ❌ | — | — |
| `description` | `"text"` | ✅ | — | — |
| `created_at` | `timestamp with time zone` | ✅ | `"now"()` | — |
| `user_id` | `"uuid"` | ✅ | — | FK → `auth.users(id)` |
| `updated_at` | `timestamp with time zone` | ❌ | `"now"()` | — |

### RLS Policies

- DELETE: Enable delete for users based on user_id
- INSERT: Enable insert for authenticated users only
- SELECT: Collaborators can view shared boards
- SELECT: Enable users to view their own data only
- UPDATE: Collaborators can update shared boards
- UPDATE: Update_Policy

---

## `task_activities`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `task_id` | `bigint` | ❌ | — | FK → `public.tasks(id)` ON DELETE CASCADE |
| `activity_id` | `bigint` | ❌ | — | FK → `public.activities(id)` ON DELETE CASCADE |
| `position` | `smallint` | ❌ | `0` | — |
| `created_at` | `timestamp with time zone` | ❌ | `"now"()` | — |
| `completed` | `boolean` | ❌ | `false` | — |

### RLS Policies

- DELETE: Collaborators can delete shared board task activities
- DELETE: Users can delete their own task activities
- INSERT: Collaborators can insert shared board task activities
- INSERT: Users can insert their own task activities
- SELECT: Collaborators can view shared board task activities
- SELECT: Users can view their own task activities
- UPDATE: Collaborators can update shared board task activities
- UPDATE: Users can update their own task activities

---

## `tasks`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `id` | `bigint` | ❌ | — | 🔑 PK, GENERATED BY DEFAULT AS IDENTITY |
| `name` | `character varying` | ✅ | — | — |
| `description` | `"text"` | ✅ | — | — |
| `created_at` | `timestamp with time zone` | ❌ | `"now"()` | — |
| `last_edited` | `timestamp without time zone` | ✅ | `"now"()` | — |
| `completion_status` | `smallint` | ✅ | `'0'::smallint` | — |
| `user_id` | `"uuid"` | ✅ | — | — |
| `type` | `smallint` | ✅ | — | — |
| `board_id` | `bigint` | ✅ | — | FK → `public.boards(id)` |
| `boards_lists_id` | `bigint` | ✅ | — | FK → `public.board_lists(id)` ON DELETE SET NULL |
| `status` | `"public"."task_status"` | ❌ | `'Initiated'::"public"."task_status"` | — |

### RLS Policies

- DELETE: Collaborators can delete shared board tasks
- DELETE: Enable delete for users based on user_id
- INSERT: Collaborators can insert shared board tasks
- INSERT: Enable insert for authenticated users only
- SELECT: Collaborators can view shared board tasks
- SELECT: Enable users to view their own data only
- UPDATE: Collaborators can update shared board tasks
- UPDATE: Enable update for authenticated users

---

## `user_preferences`

🔒 **Row Level Security enabled**

| Column | Type | Nullable | Default | Notes |
|--------|------|:--------:|---------|-------|
| `user_id` | `"uuid"` | ❌ | — | 🔑 PK, FK → `auth.users(id)` ON DELETE CASCADE |
| `app_title` | `character varying(100)` | ❌ | `'LifePlanner'::character varying` | — |
| `updated_at` | `timestamp with time zone` | ❌ | `"now"()` | — |
| `display_name` | `character varying(100)` | ✅ | — | — |
| `address` | `"text"` | ✅ | — | — |
| `avatar_url` | `"text"` | ✅ | — | — |
| `board_sort` | `character varying(20)` | ✅ | `'created_at'::character varying` | — |

### RLS Policies

- INSERT: Users can insert their own preferences
- SELECT: Users can view their own preferences
- UPDATE: Users can update their own preferences

---

